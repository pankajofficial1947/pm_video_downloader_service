###############################################################################
# Video Downloader Service
# Requirements File
#
# Install using:
#     pip install -r requirements.txt
###############################################################################

# Web framework / server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Download engine - wraps yt-dlp, which needs a system ffmpeg install
# (not a pip package) to merge separate video/audio streams and to
# extract audio-only downloads. See setup.sh / README.md.
yt-dlp>=2024.8.6

pydantic>=2.7.0
python-dotenv>=1.0.0

# Needed by Starlette's StaticFiles (serves src/static/) and FileResponse.
aiofiles>=23.2.1

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0
