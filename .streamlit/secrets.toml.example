# Copy this file to .streamlit/secrets.toml for local runs (that file
# is gitignored - never commit your real password).
#
# When deployed to Streamlit Community Cloud, set this same key under
# your app's "Settings -> Secrets" in the Streamlit Cloud dashboard
# instead - it is never read from this repo in production.

app_password = "change-me"
